// preload
function preload() {

  // load data here
  // for example images, sounds, JSON files...

}

// setup
function setup() {

  // canvas
  createCanvas(1024, 768);

}

// draw
function draw() {

  // clear background
  background(0);

  // draw HERE
  fill(255);
  rectMode(CENTER);
  rect(width / 2, height / 2, 100, 100);

}